**Презентация решения:** [prodready.pdf](prodready.pdf)

# ProdReady.pro – Автоматизация проектирования исследований биоэквивалентности

Полнофункциональный backend-сервис для автоматизированного проектирования исследований биоэквивалентности с интеграцией PubMed, LLM-анализом и генерацией отчетов.

## Возможности

- **Поиск параметров** – Интеграция с PubMed для поиска фармакокинетических параметров
- **LLM анализ** – Автоматическая экстракция Cmax, AUC, T1/2, Tmax, CV_intra с помощью YandexGPT
- **Расчеты** – Вычисление размера выборки, периода вымывания, и других параметров исследования
- **Регуляторная проверка** – Валидация соответствия требованиям ЕАЭС
- **Генерация отчетов** – Создание синопсиса исследования в формате DOCX

## Архитектура

**Stack:** FastAPI + PostgreSQL + Docker Compose
**Дополнительные сервисы:** Redis, Qdrant (vector DB), MinIO (S3)

### Основные модули

```
backend/
├── main.py                     # FastAPI приложение (API эндпоинты)
├── core/
│   ├── parsing_module.py       # Оркестрация PubMed + LLM
│   ├── design_module.py        # Генерация дизайна исследования
│   ├── regulatory_module.py    # Регуляторная проверка
│   └── report_module.py        # Генерация DOCX
└── services/
    ├── pubmed.py               # PubMed E-Utilities API
    ├── llm_client.py           # YandexGPT интеграция
    └── calculator.py           # Формулы расчетов БЭ
```

## Быстрый старт

### Важно о .env файлах

В проекте используются два файла переменных окружения:

- `.env` в корне — для Docker Compose. Содержит переменные для всех сервисов (например, DATABASE_URL, YANDEX_GPT_API_KEY и др.).
- `.env` в папке `backend` — для локального запуска Python-приложения. Может содержать только необходимые переменные для backend (см. пример в `backend/.env.example`).

Файлы могут отличаться по составу переменных. Не забывайте редактировать оба файла под свой сценарий запуска.

### Быстрый старт через Docker Compose (рекомендуется)

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/pablodrev/bioeq_ai.git
   cd bioeq_ai
   ```
2. Скопируйте файл переменных окружения:
   ```bash
   cp .env.example .env
   ```
   Отредактируйте `.env`, указав свои значения (YANDEX_GPT_API_KEY, YANDEX_FOLDER_ID, DATABASE_URL и др.).
3. Запустите сервисы:
   ```bash
   docker compose up --build
   ```
4. Откройте Swagger UI: [http://localhost:8000/docs](http://localhost:8000/docs)

### Ручная установка (без Docker)

1. Установите Python 3.10+ и PostgreSQL 15+.
2. Перейдите в папку backend:
   ```bash
   cd backend
   ```
3. Скопируйте файл переменных окружения:
   ```bash
   cp .env.example .env
   ```
   Отредактируйте `.env`, указав свои значения (YANDEX_GPT_API_KEY, YANDEX_FOLDER_ID, DATABASE_URL и др.).
4. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
5. Запустите PostgreSQL (можно через Docker):
   ```bash
   docker run -d --name pharma-db -e POSTGRES_PASSWORD=password \
     -e POSTGRES_DB=pharma_mvp -p 5432:5432 postgres:15
   ```
6. Запустите сервер:
   ```bash
   python main.py
   ```
7. Swagger UI будет доступен на [http://localhost:8000/docs](http://localhost:8000/docs)

# 4. Swagger UI

http://localhost:8000/docs

````

## 📋 API Endpoints

| Метод | Путь                                  | Описание                          |
| ----- | ------------------------------------- | --------------------------------- |
| POST  | `/api/v1/search/start`                | Начать поиск параметров по МНН    |
| GET   | `/api/v1/search/results/{project_id}` | Получить результаты поиска        |
| POST  | `/api/v1/design/generate`             | Сгенерировать дизайн исследования |
| GET   | `/api/v1/report/{project_id}`         | Скачать отчет (DOCX)              |

## ⚙️ Конфигурация

Создайте `.env` файл:

```env
YANDEX_GPT_API_KEY=your_key
YANDEX_FOLDER_ID=your_folder_id
DATABASE_URL=postgresql://user:password@localhost:5432/pharma_mvp
````

## 📚 Документация

- [START_HERE.md](./START_HERE.md) – Инструкции для разработчика
- [QUICKSTART.md](./QUICKSTART.md) – Полный гайд настройки
- [backend/README_BACKEND.md](./backend/README_BACKEND.md) – Документация API

## 🧪 Тестирование

```bash
# Интеграционные тесты (проверка сервисов)
pytest backend/test_integration.py

# Health-check тесты (MinIO, PostgreSQL, Redis, Qdrant)
pytest tests/on_wake_up/
```

## Архитектура системы

### Слоистая архитектура

```
┌─────────────────────────────────────────┐
│           FastAPI REST API              │
│  (main.py: маршруты, обработчики)      │
└──────────────┬──────────────────────────┘
               │
      ┌────────┴────────┐
      ▼                 ▼
┌─────────────┐  ┌──────────────┐
│  Core Logic │  │   Services   │
└─────────────┘  └──────────────┘
      │                  │
      ├─ parsing_        ├─ pubmed.py
      │  module.py       │  (E-Utilities API)
      │  (PubMed+LLM)    │
      │                  ├─ llm_client.py
      ├─ design_         │  (YandexGPT API)
      │  module.py       │
      │  (Расчеты)       └─ calculator.py
      │                    (БЭ формулы)
      ├─ regulatory_
      │  module.py
      │  (Проверка)
      │
      └─ report_
         module.py
         (DOCX генерация)
         │
         └──────────┐
                    ▼
         ┌─────────────────────┐
         │  PostgreSQL / Files │
         │  (БД + Reports)     │
         └─────────────────────┘
```

### Компоненты

| Компонент          | Функция                            | Технология              |
| ------------------ | ---------------------------------- | ----------------------- |
| **main.py**        | Маршруты API, управление проектами | FastAPI                 |
| **Parsing Module** | Оркестрация поиска и экстракции    | PubMed API + YandexGPT  |
| **Design Module**  | Расчет параметров дизайна          | Биостатистика (Python)  |
| **Report Module**  | Генерация синопсиса                | python-docx + YandexGPT |
| **Database**       | Хранилище результатов              | PostgreSQL + SQLAlchemy |

---

## Логика работы

### 1. Инициация поиска (асинхронно)

```
POST /api/v1/search/start {inn_en, dosage, form, ...}
  ↓
Создание проекта в БД (status=searching)
  ↓
Запуск фонового задания (BackgroundTask)
  ↓
Исполнение ParsingModule.extract_all_parameters()
```

### 2. Модуль парсинга (ParsingModule)

```
A. Основной поиск в PubMed
   └─ Поиск статей: "{INN}" AND pharmacokinetics/bioequivalence
   └─ Извлечение 5 абстрактов (top relevant)
   └─ LLM экстракция из каждого: CV_intra, Cmax, AUC, T1/2, Tmax
   └─ Сохранение в DBDrugParameter

B. Целевой поиск CV_intra (если не найдена)
   └─ Второй поиск с focus terms: "bioequivalence crossover", "intra-subject CV"
   └─ Специализированная LLM экстракция только CV_intra
   └─ Scoring по релевантности (наличие "crossover", "healthy volunteers" и т.д.)

C. Слияние результатов
   └─ Агрегация по параметрам
   └─ Сохранение в project.search_results
   └─ Обновление status → "completed"
```

**Ключевой момент:** CV_intra критический параметр — если не найдена, поиск повторяется.

### 3. Генерация дизайна (DesignModule)

```
Входные данные: CV_intra, T1/2, Tmax (из поиска выше)
  ↓
├─ Выбор дизайна: (CV_intra → "2x2 crossover" или "4-way replicate")
├─ Расчет размера выборки: n = 2 * ((z_α + z_β) / log(θ₂/θ₁))² * σ²
├─ Расчет периода вымывания: washout = 5 × T1/2
└─ Итоговые параметры дизайна
  ↓
Сохранение в project.design_parameters
```

### 4. Регуляторная проверка (RegulatoryModule)

Проверка соответствия ЕАЭС (Decision #85):

- Дизайн исследования (crossover/replicate)
- Размер выборки (N ≥ 12)
- Статистическая мощность ≥ 80%
- Критерии включения/исключения
- Биоаналитическая валидация

### 5. Генерация отчета (ReportModule)

```
Шаблон template.md + Данные проекта
  ↓
Замена параметров: {CV_INTRA}, {SAMPLE_SIZE}, {WASHOUT_DAYS}
  ↓
LLM заполнение неизвестных полей (с контекстом документа)
  ↓
Генерация DOCX с цветовым кодированием:
   ├─ Синий: точные данные из БД
   └─ Оранжевый: сгенерировано ИИ
  ↓
Сохранение в /backend/reports/
```

---

## Допущения и предположения

### 1. Параметры исследования

- **CV_intra** описывает внутрииндивидуальную вариабельность параметра
- **Минимальный размер выборки:** N = 12 (6 на период в 2×2 кроссовере)
- **Статистическая мощность:** 80% (стандарт для БЭ)
- **Уровень значимости:** α = 0.05 (двусторонний тест)
- **Границы БЭ:** θ₁ = 0.80, θ₂ = 1.25 (стандарт ЕАЭС, EMA, FDA)
- **Истинное отношение средних:** θ₀ = 0.95 (консервативное)

### 2. Источники данных

- **Статьи в PubMed:** предполагается наличие достаточных публикаций по фармакокинетике
- **Здоровые добровольцы:** поиск ограничен только исследованиями на здоровых (исключены больные)
- **Доверие к опубликованным параметрам:** принимаются высокие стандарты медицинских журналов

### 3. Дизайн исследования

- **Выбор дизайна:** зависит от CV_intra
  - CV < 20% → 2×2 crossover (4 периода кровь)
  - CV ≥ 20% → 4-way replicate (больше периодов, учитывает вариабельность)
- **Период вымывания:** washout = 5 × T₁/₂ (обеспечивает 96.8% элиминацию)

### 4. LLM извлечение

- **YandexGPT способна** правильно извлекать числовые значения и единицы из английских абстрактов
- **Параметры с типичными абиением:** Cmax, AUC, T1/2, Tmax, CV_intra распознаются стандартно

---

## Зоны для развития

Система содержит MVP функционал и подготовлена к расширению:

- **Расширение источников данных** – поддержка поиска в DrugBank, ГРЛС, или локальных баз данных от партнеров

- **Интеграция с другими LLM** – использование нескольких моделей для консенсуса

- **Асинхронные воркеры** – использование Celery/RabbitMQ для параллельных поисков

- **Qdrant интеграция** – поиск похожих исследований через векторные эмбеддинги

---
